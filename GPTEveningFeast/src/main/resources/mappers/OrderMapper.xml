<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="kosa.hdit5.evenapp.mapper.OrderMapper">
   <select id="selectPreOrderProduct" statementType="CALLABLE" resultType="Cart">
<!--        {call #{return_cart, mode=OUT, jdbcType=CURSOR, resultMap=Cart}= selectPreOrderProduct(#{arr_cart,jdbcType=ARRAY,typeHandler=CustomTypeHandler})} -->
   </select>
</mapper>  

